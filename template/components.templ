package template

templ InputField(title string,name string, required bool) {
	<div class="mb-3">
		<label for="name" class="form-label">{ title }</label>
		<input type="text" id="name" name={ name } class="form-control input input-bordered input-info" required?={ required }/>
	</div>
}
